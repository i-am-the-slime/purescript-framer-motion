{
  "name": "examples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "http-server",
    "bundle": "esbuild entry.js --bundle --outfile=bundle.js",
    "watch": "esbuild entry.js --bundle --outfile=bundle.js --watch",
    "build": "spago build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "framer-motion": "^5.3.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.1",
    "html-server": "^0.1.1",
    "purescript": "^0.14.5",
    "spago": "^0.20.3"
  }
}
